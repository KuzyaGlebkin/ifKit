## 1. Типы и нормализация локалей

- [x] 1.1 Ввести тип(ы) для бандла `game` + `ui` per language; обновить `GameConfig` / экспорты; сохранить обратную совместимость с плоским `Record` как только `game`.
- [x] 1.2 Реализовать `normalizeLocales` / merge: плоский формат → `{ game, ui: merge(builtInUi[lang], authorUi) }`; для `sourceLanguage` пустой `game` не обнулять `ui`.
- [x] 1.3 Централизовать константы **ключей** системных строк (`UI_KEYS` или аналог) согласно списку из `settings-modal`, `saves-modal`, `index.html` (#controls).

## 2. i18n: u(), resolved lang, document.documentElement.lang

- [x] 2.1 Хранить в модуле i18n `ui` по языкам; реализовать `u(key: string): string` с цепочкой fallback (автор → встроенный → primary subtag/дефолт).
- [x] 2.2 Добавить встроенные словари `ui` **минимум** для `ru` и `en` (все обязательные ключи).
- [x] 2.3 Использовать **один** resolved language tag для `t` и `u`; в `setLanguage` / `setLanguageOrAuto` / `initI18n` **синхронно** обновлять `document.documentElement.lang`.
- [x] 2.4 Убедиться, что `applySettings` (или хук после неё) не рассинхронизирует `lang` с i18n при старте, сбросе, импорте.

## 3. engine и defineGame

- [x] 3.1 В `defineGame` передавать в `initI18n` нормализованные `fullLocales` **с** `ui` слоем; вызывать обновление `lang` сразу после init.
- [x] 3.2 Публичный re-export `u` из `ifKit` (при необходимости для авторов) — по решению, минимум внутреннее использование.

## 4. Модалка настроек

- [x] 4.1 Заменить жёсткие строки в `settings-modal.ts` на `u(...)`; кнопки «Авто» и коды языков — по правилам из design.
- [x] 4.2 Реализовать **обновление** подписей при смене языка при открытой модалке (перерисовка/обновление DOM).

## 5. Модалка сохранений и панель

- [x] 5.1 Заменить жёсткие строки в `saves-modal.ts` на `u(...)`; подтверждения «Да/Нет» и заголовок — в словаре.
- [x] 5.2 Инициализация: после первого i18n — выставить подписи `#btn-undo` / `#btn-redo` / `#btn-saves` / `#btn-settings` из `u` (модуль toolbar или `engine.ts`); при смене языка — обновлять.
- [x] 5.3 `index.html`: согласовать **начальный** `lang` (например `en` или `und`) + комментарий, что движок перезаписывает; не оставлять долговечно `ru`, если игра — `en`.

## 6. Спеки и проверка

- [x] 6.1 Синхронизировать `openspec/specs/` (дельта-команда или ручной перенос после завершения change — по процессу проекта); прогнать сценарии вручную: смена `ru`↔`en`, авто, импорт настроек, `lang` + скринридер smoke.
