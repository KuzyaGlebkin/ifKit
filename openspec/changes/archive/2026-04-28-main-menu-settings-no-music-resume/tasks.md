## 1. Обход полного цикла сцены на сессионном меню

- [x] 1.1 В `scenes.ts` (или связанном модуле) добавить явный способ различать «полная перерисовка с аудио» и «обновление без аудио при активном `ifk-session-menu-active`» — например функцию `rerenderForSettingsOrI18n()`, вызывающую полный `runGameLoop` только если сессионное главное меню не активно.
- [x] 1.2 Если для смены языка или сброса seen на меню нужно точечно обновить подсветку/seen без полного цикла — вызвать только необходимые функции (`markAndHighlight` и т.п.), не выполняя тело сцены с `PlayMusic`.
- [x] 1.3 В `settings-modal.ts` заменить прямые вызовы `rerender()` после смены языка и после сброса seen на новый путь из п. 1.1–1.2.

## 2. Проверка и регрессии

- [x] 2.1 Вручную: игра → бренд → главное меню → настройки → смена языка и сброс seen — музыка не должна включаться; «Новая игра» / вход в игру — музыка снова ведёт себя как раньше.
- [x] 2.2 Вручную: те же действия в настройках **во время игры** (не сессионное меню) — локаль и seen обновляются, музыка не ломается.
