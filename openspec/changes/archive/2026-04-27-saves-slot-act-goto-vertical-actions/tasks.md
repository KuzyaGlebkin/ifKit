## 1. Модель данных и сохранение

- [x] 1.1 Расширить `SaveSlot` в `src/ifKit/saves.ts` полями `actPreview` и `gotoPreview` (string); при записи в `saveToSlot` заполнять из первого `.btn-act` в `#scene-acts` и первого `.btn-goto` в `#scene-gotos` (`textContent`, `trim`, обрезка до 80 символов по аналогии с `label`).
- [x] 1.2 В `autoSave` задавать `actPreview: ''` и `gotoPreview: ''`; при необходимости нормализовать уже загруженные слоты в `loadSavesStore` (подставить `''`, если ключей нет).
- [x] 1.3 Убедиться, что типы и сериализация в `localStorage` не ломают существующие сохранения.

## 2. Модалка сохранений

- [x] 2.1 В `src/ifKit/saves-modal.ts` для занятого именного слота выводить в `.ifk-slot-info` строки для непустых `actPreview` и `gotoPreview` (разметка и `escapeHtml`); не показывать пустые строки.
- [x] 2.2 Проверить `aria-labelledby` / группу слота: новые строки остаются внутри карточки без конфликта с существующими id.
- [x] 2.3 Режимы подтверждения удаления и перезаписи: разметка в `.ifk-slot-actions` остаётся читаемой при вертикальной колонке.

## 3. Стили

- [x] 3.1 В `src/style.css` задать для `.ifk-slot-actions` вертикальную колонку (`flex-direction: column`, выравнивание на всю ширину колонки), стили для новых строк превью (ellipsis, наследование от `.ifk-slot-label` или отдельный класс).

## 4. Проверка

- [x] 4.1 Ручная проверка: именной слот с act и goto — три строки превью; слот только с goto; старое сохранение без полей; узкая ширина и иконки на кнопках (≤600px) совместимы с вертикальным столбцом.
