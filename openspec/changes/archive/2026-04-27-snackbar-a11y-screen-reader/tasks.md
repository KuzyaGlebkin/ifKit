## 1. Разметка live region

- [x] 1.1 В `initSnackbar` (`src/ifKit/snackbar.ts`) после создания `#snackbar-container` задать `role="status"` и `aria-live="polite"` на контейнере (не на `.snackbar-item`).

## 2. Проверка

- [x] 2.1 Собрать/запустить приложение, вызвать сцену с `watch`, убедиться в DOM-атрибутах контейнера.
- [x] 2.2 При наличии скринридера — убедиться, что текст уведомления объявляется после действия (polite, без обязательного прерывания).
