## 1. Стили snackbar

- [x] 1.1 В `src/style.css` добавить правила для `#snackbar-container`: фиксированное положение у нижнего края вьюпорта, горизонтальное центрирование (flex или аналог без проблем с `position: fixed` у потомков), безопасные отступы от краёв (`padding-inline`, при необходимости `env(safe-area-inset-bottom)`), column stack с зазором между плашками, `z-index` ниже модалок (`1000`), достаточный для отображения над сценой.
- [x] 1.2 Стилизовать `.snackbar-item`: `border-radius: var(--ifk-radius)`; `padding` согласованный с плотностью UI; `background`, `color`, `border` через токены (`--ifk-color-surface`, `--ifk-color-text`, `--ifk-color-border`); при необходимости лёгкая `box-shadow`; ограничение `max-width` (например `min(100%, var(--ifk-max-width))` или `calc(100vw - 2*spacing)`), чтобы длинный текст не ломал вёрстку.
- [x] 1.3 Вручную проверить в dev: светлая/тёмная тема, несколько подряд уведомлений (пример в `src/game.ts` + действия в сцене), отсутствие перекрытия с модалкой настроек/сейвов по ожидаемому приоритету (snackbar снизу, модалка — поверх).
