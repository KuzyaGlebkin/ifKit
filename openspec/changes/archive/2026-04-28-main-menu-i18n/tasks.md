## 1. Разрешение игровых строк для меню

- [x] 1.1 В `src/ifKit/i18n.ts` добавить и экспортировать `translateGameString(sourceKey: string): string` с той же семантикой поиска и fallback, что у `t` без подстановок (ключ — полная строка `sourceKey`).
- [x] 1.2 В `src/ifKit/session-main-menu.ts` сохранять переданные `title` и `description` в модульном состоянии; при установке текста и в `refreshSessionMainMenuChrome()` подставлять в `#ifk-session-menu-title-text` и `#ifk-session-menu-description` результаты `translateGameString`, сохраняя логику `hidden` для пустого описания после перевода.

## 2. Образцовая игра и ключи локали

- [x] 2.1 Добавить в `src/locales/en.game.json` переводы для строк `meta.title` и `meta.description` из `src/game.ts` (ключи — точные русские строки-источники).
- [x] 2.2 При необходимости дополнить `src/locales/template.game.json` теми же ключами для консистентности с `i18n:extract` (если шаблон поддерживает такие записи в проекте).

## 3. Проверка

- [x] 3.1 Вручную: стартовое меню на русском и английском показывает согласованные заголовок, описание и кнопки; смена языка в настройках обновляет заголовок и описание без перезагрузки.
