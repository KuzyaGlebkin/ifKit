## 1. Схема настроек и хранилище

- [x] 1.1 Удалить `quietMusicForScreenReader` из типа `Settings`, `engineDefaults`, `loadSettings`/мержа и любых дубликатов схемы; добавить `masterVolume` (0–1) и `masterMuted`; игнорировать устаревший ключ при чтении JSON.
- [x] 1.2 Обновить `saveSettings`/`resetSettings`/импорт так, чтобы новые поля сохранялись и сбрасывались вместе с остальными; убедиться, что при следующей записи в хранилище нет `quietMusicForScreenReader`.
- [x] 1.3 Пройтись по вызовам API настроек (`engine.ts`, тесты, примеры игр), удалить обращения к `quietMusicForScreenReader`.

## 2. Аудиодвижок

- [x] 2.1 Ввести общий выходной `GainNode`, подключить к нему каналы музыки и звуков согласно `design.md`; удалить константу ослабления α, внутренний флаг и `setQuietMusicForScreenReader`.
- [x] 2.2 Реализовать `setMasterVolume` / `setMasterMuted` и пересчёт эффективного выхода; на инициализации применять значения из текущих настроек.
- [x] 2.3 Обновить все места, где перечислялись сохраняемые поля при стартовом меню / intent (не трогать пользовательские значения в хранилище).

## 3. Модалка настроек

- [x] 3.1 Удалить разметку и обработчики переключателя «Тихая музыка»; убрать вызовы `setQuietMusicForScreenReader`.
- [x] 3.2 Добавить строку «Громкость» между блоком акцента и строкой «Музыка»: чекбокс `masterMuted`, ползунок `masterVolume`, проценты и `aria-valuetext` по образцу музыки/звуков; обновить порядок Tab.
- [x] 3.3 Обновить синхронизацию UI при открытии, сбросе, импорте и `refreshSettingsModalI18n`.

## 4. Локализация

- [x] 4.1 Заменить ключи UI для тихой музыки на ключи для общей громкости в `builtin-ui` / типах `UiKey` и в `*.ui.json`; расширить шаблон процентов для ползунка общей громкости при необходимости.
- [x] 4.2 Проверить `author-ui-strings` / извлечение ключей, если затронуты скрипты.

## 5. Спеки и регрессия

- [x] 5.1 После реализации заархивировать дельты через workflow OpenSpec (`openspec archive`), когда код готов.
- [x] 5.2 Прогнать существующие тесты и ручную проверку: три строки громкости, сброс, экспорт/импорт, воспроизведение музыки и звука при разных комбинациях mute.
