## 1. Импорты и фоновая музыка

- [x] 1.1 В `src/scenes.ts` добавить Vite-импорты `Adventure.mp3`, `impactGeneric_light_001.ogg`, `impactWood_heavy_001.ogg` из `src/assets/`
- [x] 1.2 Раскомментировать и использовать `PlayMusic` в импорте из `./ifKit` (как остальные теги)
- [x] 1.3 Вызвать `PlayMusic(adventureMusic)` в теле сцен `start` и `clearing` с одним и тем же импортом, чтобы фон не обрывался при переходе

## 2. Звуки действий

- [x] 2.1 В колбэке `act` для «Подобрать монету» вызвать `Sound` с URL монеты до или после инкремента `gold`
- [x] 2.2 В колбэке `act` для «Поднять ветку» вызвать `Sound` с URL ветки при установке `hasBranch`

## 3. Проверка

- [x] 3.1 В dev: убедиться, что после первого клика слышна музыка и эффекты не глушат её; слайдеры/мьют в настройках влияют на громкость (как до изменения)
- [x] 3.2 Собрать production (или preview) и убедиться, что ассеты подхватываются (data URL / корректные пути)
