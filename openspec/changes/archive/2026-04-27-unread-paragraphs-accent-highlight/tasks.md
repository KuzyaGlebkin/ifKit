## 1. Стили

- [x] 1.1 В `src/style.css` для `.paragraph--unseen` заменить фоллбек `var(--ifk-unseen-color, …)` на формулу с `color-mix` от `var(--ifk-color-accent)` и `transparent` (согласовать процент с design, по умолчанию ~18%), убрать жёлтый `rgba(255, 220, 80, 0.18)` как встроенный дефолт.

## 2. Проверка и спеки

- [x] 2.1 Визуально проверить светлую и тёмную тему и смену пресета акцента: тинт непрочитанного абзаца следует акценту, текст остаётся читаемым.
- [x] 2.2 Убедиться, что кастомный `--ifk-unseen-color` по-прежнему полностью переопределяет фон (как в сценарии дельта-спека `ui-theming`).

- [x] 2.3 После мерджа/архивирования change: применить `openspec archive` (или согласованный процесс), чтобы дельта `ui-theming` и `snackbar` пополнила `openspec/specs/`.
