## 1. Схема и хранение настроек

- [x] 1.1 Добавить `quietMusicForScreenReader: boolean` в тип `Settings`, `engineDefaults`, нормализацию/слияние в `loadSettings` (отсутствие ключа → `false`), экспорт/импорт и `resetSettings` в `src/ifKit/settings.ts` (и связанные места, если схема дублируется).

## 2. Аудиодвижок

- [x] 2.1 Ввести константу `SCREEN_READER_MUSIC_ATTENUATION` (например `0.25`), внутренний флаг и единую функцию эффективного gain музыки: `musicVolume * (musicMuted ? 0 : 1) * (quietMusicForScreenReader ? α : 1)` в `src/ifKit/audio.ts`.
- [x] 2.2 Экспортировать `setQuietMusicForScreenReader(on: boolean): void`, вызывать `applyMasterGains` (или эквивалент); до инициализации `AudioContext` — только состояние, как для других аудиополей.
- [x] 2.3 Подключить флаг при первом создании контекста и при всех путях, обновляющих `setMusicVolume` / `setMusicMuted`.

## 3. Связка движка и UI настроек

- [x] 3.1 При старте и при изменении настроек из модалки вызывать `setQuietMusicForScreenReader` согласно текущему `settings` в `src/ifKit/engine.ts` (или точке, где уже вызываются `setMusicVolume` / `setMusicMuted`).

## 4. Модалка настроек

- [x] 4.1 Добавить чекбокс с подписью и при необходимости `aria-describedby` после строки музыки и до строки звуков; синхронизация с хранилищем и обработчики в `src/ifKit/settings-modal.ts`.
- [x] 4.2 Включить элемент в порядок Tab (после ползунка музыки, до строки звуков) и обновить сброс/импорт UI.

## 5. i18n

- [x] 5.1 Добавить стабильные ключи `u()` и строки во `builtin-ui` / `ui-keys` (русский и английский минимум); при необходимости дубли в `src/locales` примера игры.

## 6. Проверка

- [x] 6.1 Ручная проверка: при включённом флаге музыка слышна тише при том же ползунке; mute и слайдер по-прежнему не затираются; сброс и импорт восстанавливают состояние чекбокса.
