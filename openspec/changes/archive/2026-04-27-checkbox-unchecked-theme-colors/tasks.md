## 1. Стили `.ifk-checkbox`

- [x] 1.1 Реализовать в `src/style.css` оформление неотмеченного состояния: граница через токен цвета текста (например `var(--ifk-color-text)`), заливка через токен фона (например `var(--ifk-color-bg)` или согласованный выбранный в `design.md` токен); при необходимости использовать `appearance: none` и оформление `:checked` с акцентными токенами, плюс `:focus-visible` согласованно с движком.
- [x] 1.2 Проверить вручную в светлой и тёмной теме и при системной теме: модалка настроек — чекбоксы музыки, звуков и «непрочитанное» (если виден); убедиться, что отмеченное состояние и фокус остаются пригодными.

## 2. Спецификация

- [x] 2.1 После реализации перенести дельту из `openspec/changes/checkbox-unchecked-theme-colors/specs/settings-modal/spec.md` в канонический `openspec/specs/settings-modal/spec.md` согласно рабочему процессу apply (сохранить формат ADDED/MODIFIED сценариев).

## 3. Проверка

- [x] 3.1 Сборка/линт по проекту (например `npm test` / существующие скрипты в `package.json`) без новых ошибок, связанных с изменениями.
