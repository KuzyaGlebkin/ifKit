## 1. Модель настроек и загрузка

- [x] 1.1 Добавить в тип `Settings` поля `musicMuted` и `soundMuted` (`boolean`); выставить `false` в `engineDefaults` и при отсутствии ключей в `loadSettings` (слияние с `authorDefaults`).
- [x] 1.2 Убедиться, что валидация/нормализация сохранённого JSON принимает новые поля; экспорт/импорт хранилища их не теряет.

## 2. Аудиомодуль

- [x] 2.1 Хранить номинальные громкости и флаги mute (или читать из настроек при каждом применении); вычислять эффективный `gain` как произведение.
- [x] 2.2 Обновить инициализацию `AudioContext`: стартовые `_masterMusicGain` / `_masterSoundGain` с учётом mute.
- [x] 2.3 Расширить `setMusicVolume` / `setSoundVolume` пересчётом gain с учётом mute; добавить `setMusicMuted` / `setSoundMuted` (или эквивалент) с пересчётом из номинала.
- [x] 2.4 Подключить вызовы из движка при старте (`initAudioVolumes` / аналог) — передавать или забирать mute из актуальных настроек.

## 3. Модалка настроек и i18n

- [x] 3.1 Добавить стабильные ключи UI для чекбоксов, строки в `builtin-ui` (ru/en), при необходимости обновить примеры `locales.ui` в игре/доках.
- [x] 3.2 Разметка строк «Музыка» и «Звуки»: подпись — чекбокс — ползунок и проценты; связать чекбоксы с `musicMuted` / `soundMuted` и сохранением.
- [x] 3.3 Синхронизировать UI при открытии модалки, сбросе и импорте; вызывать аудио API при изменении mute и ползунков.
- [x] 3.4 Доступность: связка подписи с чекбоксом и/или `aria-label` через `u(...)`.

## 4. Спеки и проверка

- [x] 4.1 После реализации прогнать ручную проверку: mute on/off, движение слайдера при mute, перезагрузка, сброс, импорт.
- [x] 4.2 Архивировать change (`/opsx:archive`) после принятия и обновить `openspec/specs/*` при архивации по правилам проекта.
