## 1. Реализация helpers в ifKit (**BREAKING**)

- [x] 1.0 Зафиксировать вариант имён (A: `Pt`/`H1t`/… и снятие публичных `P`/`H1`/…(string); B: только tagged под именами `P`/`H1`/…).
- [x] 1.1 В `tag-functions.ts` (или рядом) добавить imperative tagged templates (`Pt`, `H1t`, … или переопределённый `P`/`H1`), вызывающие `t` затем внутренний `p`/`h1`/… и `appendHtml`.
- [x] 1.2 Добавить inline tagged templates `emt`, `strongt` (или эквивалент), возвращающие строку: `t` затем `em`/`strong`.
- [x] 1.3 **BREAKING:** убрать с публичного API прежние `P`/`H1`/… с сигнатурой `(content: string)` (или заменить на несовместимые сигнатуры), обновить `index.ts` и все внутренние/авторские вызовы. Добавлен `Phtml` для абзаца из уже собранной строки (склейка нескольких `t`).

## 2. Образцовая игра и проверка

- [x] 2.1 Перевести `src/scenes.ts` на `Pt`/`H1t`/… и `emt`/`strongt`; убрать `t` из импорта, если он больше не используется в файле; оставить `act`/`goto` с `t` в подписях, пока нет отдельного API.
- [x] 2.2 Убедиться, что `src/game.ts` (`watch`, `static`) по-прежнему корректно использует `t` и сборка (`npm run build` или эквивалент проекта) проходит.

## 3. Спеки (после реализации)

- [x] 3.1 При архивировании изменения: слить дельту `specs/i18n/spec.md` в `openspec/specs/i18n/spec.md` по правилам проекта (если движок OpenSpec требует ручного переноса — выполнить в фазе archive, не блокирует apply).
