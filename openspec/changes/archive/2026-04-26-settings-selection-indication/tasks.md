## 1. Токены и стили

- [x] 1.1 В `style.css` добавить `--ifk-segment-selected-bg` и `--ifk-segment-selected-fg` в `:root`, `[data-theme="dark"]` и при необходимости в `@media (prefers-color-scheme: dark)` согласованно с существующей схемой тем.
- [x] 1.2 Перевести правила `.ifk-theme-btn--active`, `.ifk-accent-btn--active`, `.ifk-lang-btn--active` на использование новых токенов (без единственной привязки к `--ifk-color-accent` для заливки активного сегмента).
- [x] 1.3 Проверить hover/focus-visible для сегментов: активный сегмент остаётся различимым, контраст текста приемлемый в светлой и тёмной теме.

## 2. Модалка (при необходимости)

- [x] 2.1 Если после 1.x индикации недостаточно (см. `design.md`): добавить в разметку `settings-modal.ts` элементы для краткого текста текущего значения у «Тема» / «Акцент» / «Язык» и обновлять их в `syncToDOM`. *(Проверено: достаточно токенов и hover-состояния; разметка не менялась.)*
- [x] 2.2 Убедиться, что импорт/сброс настроек и открытие модалки синхронизируют индикацию со `Settings`.

## 3. Верификация

- [x] 3.1 Ручная проверка сценариев из дельта-спеки: все пресеты акцента, обе темы, язык «Авто» и явный код.
- [x] 3.2 При архивации изменения — слить дельта-спеки в `openspec/specs/` по процессу проекта.
