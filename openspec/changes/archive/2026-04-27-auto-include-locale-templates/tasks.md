## 1. Данные и скрипты

- [x] 1.1 Добавить `src/locales/template.game.json` и `src/locales/template.ui.json` (начальное содержимое согласовать с текущими ключами образцовой игры — миграция из `en.*` при необходимости).
- [x] 1.2 Переключить `scripts/extract-game-i18n.mjs` на вывод в `template.game.json`; убедиться, что первый запуск создаёт файл при отсутствии.
- [x] 1.3 Проверить `extract-ui-i18n.mjs`: при наличии `template.game.json` обновляется `template.ui.json`; при отсутствии `*.game.json` задокументировать/исправить краевой случай.

## 2. Движок (ifKit)

- [x] 2.1 Добавить модуль (например `locale-templates.ts`), статически импортирующий `../locales/template.game.json` и `../locales/template.ui.json`.
- [x] 2.2 В `engine.ts` перед `normalizeLocalesInput` подмешивать шаблон к каждому авторскому языковому слою (`game` / `ui`) с перекрытием автором, без добавления кода `template` в список языков.
- [x] 2.3 В `i18n.ts` обновить `t`: пустая строка в активной игровой локали трактуется как отсутствие перевода (как отсутствие ключа).

## 3. Образец и проверка

- [x] 3.1 Упростить `src/game.ts`: убрать импорты, нужные только для каталога ключей; оставить только реальные языки образцовой игры (`ru` / `by` / др. по факту).
- [x] 3.2 Запустить `npm run i18n:extract`, `npm run build` и при необходимости `npm run build:tauri-web`; убедиться, что в настройках нет лишних языков и `t` / `u` работают.
- [x] 3.3 Обновить упоминания `en.game.json` как единственной цели extract в пользовательской документации репозитория, если они есть и входят в объём change.
