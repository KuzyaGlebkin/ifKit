## 1. Панель `#controls`

- [x] 1.1 Обновить разметку в `src/index.html`: в каждой из `#btn-undo`, `#btn-redo`, `#btn-saves`, `#btn-settings` — контейнер под SVG и span с текстовой подписью (класс для скрытия на узком экране).
- [x] 1.2 Добавить модуль или фрагмент инициализации (например вызов из `game.ts` / точки старта UI): импорт `createElement`, `Undo`, `Redo`, `Save`, `Settings` из `lucide`; вставка иконки в контейнер с `aria-hidden="true"`, корректные `width`/`height`/`stroke` через класс CSS.
- [x] 1.3 В `src/style.css`: стили иконки в кнопке; медиазапрос `max-width: 600px` — скрыть видимый текст подписей `#controls`, задать `aria-label` на кнопках (или `sr-only` текст — согласно выбранному в коде подходу из `design.md`).

## 2. Модалка сохранений

- [x] 2.1 Найти место сборки кнопок «Загрузить» / «Сохранить» / «Удалить» (например `src/ifKit/saves.ts`) и добавить ту же схему «контейнер иконки + подпись».
- [x] 2.2 Импорт `ArrowUpFromLine`, `ArrowDownToLine`, `Trash` и вставка через `createElement`; при перерисовке слотов сохранять доступные имена кнопок на узкой ширине.
- [x] 2.3 Те же медиастили (или общий класс) для icon-only при `max-width: 600px` внутри модалки.

## 3. Проверка

- [x] 3.1 Ручная проверка: широкий и узкий viewport — иконки, текст, Tab-фокус, озвучивание с NVDA/VoiceOver по возможности; `npm run build` без ошибок.
