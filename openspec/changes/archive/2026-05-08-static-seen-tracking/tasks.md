## 1. Хранилище seen

- [x] 1.1 Расширить `seen-content.ts`: загрузка/сохранение только `{ scenes, staticSeen }`; при неверной форме трактовать как пустые данные без миграции legacy
- [x] 1.2 Обновить `markAndHighlight`: регионы по `data-ifk-seen-scope`, глобальные хеши для static, legacy-плоское дерево без обёрток
- [x] 1.3 Обновить `resetSeenContent` под полный сброс обеих областей хранилища

## 2. Сборка HTML и связка со сценой

- [x] 2.1 В `composeScene` / `flushComposedToDOM` (или соседний слой): при наличии `static` генерировать обёртки before / scene / after с маркировкой регионов (`data-ifk-seen-scope`), опускать пустые части
- [x] 2.2 При отсутствии `static` не менять внешний вид DOM (нет лишних обёрток)
- [x] 2.3 Обновить `style.css`: при обёртках добавить интервалы внутри `.ifk-seen-scope > * + *` рядом с `#scene-content > * + *` (обёртки — обычные `div`, без `display: contents`)

## 3. Верификация

- [x] 3.1 Ручная проверка сценария демо-игры: Лес → монета → Поляна без ложной подсветки строки статистики при неизменном HTML
- [x] 3.2 Автоматических тестов на seen в проекте нет; задача считается н/п при отсутствии раннеров — проверка через `npm run build` и смоук-сценарий в браузере
