## 1. История и Tab (`ui-buttons`)

- [x] 1.1 Заменить исключение `#btn-undo` / `#btn-redo` из Tab через `disabled` на подход с сохранением фокуса (`aria-disabled`, визуальный disabled, блокировка активации в обработчике).
- [x] 1.2 Проверить порядок Tab в `#controls` и при необходимости скорректировать разметку/стили фокуса без изменения визуального порядка кнопок.

## 2. Модалка сохранений (`saves-modal`)

- [x] 2.1 Реализовать удержание фокуса внутри модалки сохранений (совместно с `settings-modal`: общий хелпер или дублирование по паттерну из design.md — `inert` с fallback).
- [x] 2.2 При открытии/закрытии сохранять и восстанавливать фокус на `#btn-saves` (или актуальный триггер).
- [x] 2.3 Связать `ifk-slot-num` и `ifk-slot-label` для AT: `role="group"` + `aria-labelledby` (или эквивалент), не ломая визуал карточки.

## 3. Модалка настроек (`settings-modal`)

- [x] 3.1 Реализовать удержание фокуса внутри модалки настроек и возврат фокуса на `#btn-settings`.
- [x] 3.2 Обернуть секции «Тема» и «Шрифт» в контейнер с `aria-hidden="true"`; убедиться, что при открытии модалки фокус не остаётся внутри скрытой области.
- [x] 3.3 Добавить для секции языка `fieldset`/`legend` или `role="group"` + `aria-labelledby`, чтобы контекст совпадал по принципу с «Музыка»/«Звуки».

## 4. Фокус после смены сцены (`ui-zones` / engine)

- [x] 4.1 Добавить перед `#scene-content` пустой `#ifk-scene-focus-anchor` с `tabindex="-1"` (вне цепочки Tab), вставка в `engine.ts`.
- [x] 4.2 После рендера при смене `sceneKey` вызывать `focus({ preventScroll: true })` на якорь; не сбивать фокус при `rerender` без смены сцены (см. сценарии спеки).

## 5. Проверка

- [x] 5.1 Ручная проверка: Tab с открытой модалкой сохранений / настроек не даёт сфокусировать вторую кнопку модалки.
- [x] 5.2 Ручная проверка скринридером (NVDA/JAWS/VoiceOver по доступности): слоты, язык, пропуск темы/шрифта, переход сцены с чтением вниз от пустого элемента перед сценой.
