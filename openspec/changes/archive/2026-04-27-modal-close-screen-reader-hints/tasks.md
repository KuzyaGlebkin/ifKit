## 1. Строки и ключи UI

- [x] 1.1 Добавить ключи в `ui-keys.ts` (например `settingsClose`, `savesClose`) и переводы в `builtin-ui.ts` (RU/EN), формулировки в духе «Закрыть настройки» / «Закрыть сохранения».

## 2. Модалка настроек

- [x] 2.1 На `#ifk-settings-close` задать `data-ifk-ui-aria` с новым ключом; убедиться, что `refreshSettingsModalI18n()` выставляет `aria-label` при инициализации и смене языка.

## 3. Модалка сохранений

- [x] 3.1 На `#ifk-saves-close` задать `data-ifk-ui-aria` с ключом сохранений.
- [x] 3.2 В `refreshSavesModalChrome()` обновлять `aria-label` для элементов с `data-ifk-ui-aria` внутри backdrop (по образцу настроек).

## 4. Проверка

- [x] 4.1 Вручную: открыть обе модалки, проверить в дереве доступности/скринридере имя кнопки закрытия; сменить язык и повторить.
