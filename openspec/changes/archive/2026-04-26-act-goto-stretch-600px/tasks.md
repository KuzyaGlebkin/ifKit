## 1. Стили сценовых кнопок

- [x] 1.1 В `src/style.css` убрать `.btn-act` / `.btn-goto` (width и text-align) из блока `@media (max-width: 480px)`, оставив в нём только правила для `#content`, если они нужны.
- [x] 1.2 Добавить те же правила полной ширины и центрирования для `.btn-act` и `.btn-goto` в `@media (max-width: 600px)` (по дизайну — в существующий блок с тулбаром, с комментарием про scene buttons).

## 2. Спека и проверка

- [x] 2.1 Вручную проверить в DevTools: при ширине 600px и 481px кнопки на всю строку; при 601px — без принудительной полной ширины.
- [x] 2.2 После реализации и архивации изменения — слить дельту `openspec/changes/.../specs/ui-buttons/spec.md` в `openspec/specs/ui-buttons/spec.md` согласно процессу проекта.
