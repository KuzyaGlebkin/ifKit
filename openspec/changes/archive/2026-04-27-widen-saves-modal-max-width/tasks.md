## 1. Стили

- [x] 1.1 В `src/style.css` для селектора `.ifk-saves-dialog` заменить `max-width: 560px` на `max-width: 840px`.

## 2. Проверка

- [x] 2.1 Открыть модалку сохранений на широком viewport: ширина диалога не больше ~840px.
- [x] 2.2 На узком viewport (меньше 840px): диалог остаётся в пределах экрана, без горизонтальной полосы прокрутки из‑за модалки.
