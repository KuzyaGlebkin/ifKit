## 1. Данные локалей

- [x] 1.1 Скопировать все строки из текущего `BUILTIN_UI.en` в `src/locales/en.ui.json` как непустые значения по тем же ключам `UiKey`

## 2. Встроенный UI

- [x] 2.1 Удалить объект `en` из `src/ifKit/builtin-ui.ts`, оставить только слой языка оригинала (для примера — `ru`)

## 3. Merge и fallback в i18n

- [x] 3.1 Обновить `normalizeLocalesInput`: не использовать `BUILTIN_UI.en` как запасной базовый слой для языков без `BUILTIN_UI[lang]`; учитывать `sourceLanguage` согласно `design.md`
- [x] 3.2 Обновить `u()` и связанные места: убрать зависимость от `BUILTIN_UI.en`; согласовать порядок fallback с `specs/i18n/spec.md` (оригинал в TS + данные из `locales`)

## 4. Проверка и документация

- [x] 4.1 Ручная проверка UI на языках `ru`, `en`, `by` и режиме «Авто»
- [x] 4.2 Убедиться, что `npm run build` / тесты (если есть) проходят
