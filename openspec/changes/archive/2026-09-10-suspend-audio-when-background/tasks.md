## 1. Аудиодвижок: видимость документа

- [x] 1.1 В `audio.ts` (или на этапе создания `AudioContext`) добавить подписку на `visibilitychange`; при `document.visibilityState === 'hidden'` вызывать `_ctx?.suspend()` если контекст есть; при `'visible'` — `_ctx?.resume()` (без побочных изменений сохранённых громкостей/mute).
- [x] 1.2 Убедиться, что цикл скрытия/показа не конфликует с существующим one-shot resume на первый жест пользователя и с логикой стартового/сессионного меню (музыка не должна появляться там, где спецификация это запрещает, только потому что сработал `resume` при visible).

## 2. Проверка и документация рантайма

- [x] 2.1 Вручную проверить single HTML: вкладка в фон/передний план; при наличии устройства — мобильный переключатель приложений.
- [ ] 2.2 Проверить Tauri-сборку (или зафиксировать в задаче/issue поведение, если отличается) и при необходимости уточнить `design.md` Open Questions.


