## 1. Разметка модалки настроек

- [x] 1.1 Удалить `tabindex="-1"` у `#ifk-music-vol` и `#ifk-sound-vol` в шаблоне модалки (`settings-modal.ts` или эквивалент), чтобы ползунки участвовали в порядке Tab по умолчанию.

## 2. Проверка

- [x] 2.1 Вручную: открыть настройки → Tab от чекбокса музыки → фокус на ползунке музыки; то же для звуков; Shift+Tab возвращает на чекбокс.
- [x] 2.2 Убедиться, что цикл Tab по-прежнему остаётся внутри модалки (`lockModalFocus` / `getTabbableIn` включают оба range после правки).

## 3. Спецификация репозитория

- [x] 3.1 После реализации — прогнать архивацию/слияние дельты в `openspec/specs/settings-modal/spec.md` по принятому в проекте процессу OpenSpec.
