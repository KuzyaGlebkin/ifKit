## 1. Ключи и fallback

- [x] 1.1 Добавить стабильные ключи `UI` для `aria-valuetext` громкости (один общий шаблон `{0}` или отдельно музыка/звуки) в `ui-keys.ts`
- [x] 1.2 Добавить встроенные строки (ru/en) в `builtin-ui.ts` с плейсхолдером для 0–100
- [x] 1.3 При смене языка вызывать обновление aria для открытой модалки (уже покрывается `refreshSettingsModalI18n`, при необходимости расширить)

## 2. Модалка настроек

- [x] 2.1 Вынести единую функцию формата уровня в процентах для **видимой** подписи и для **AT** (то же целое число, что и `asPercent` / `#ifk-*-vol-val`)
- [x] 2.2 На инициализации и в `syncToDOM` задавать `aria-valuetext` для `#ifk-music-vol` и `#ifk-sound-vol` через `u(…)` + число
- [x] 2.3 В обработчиках `input` ползунков громкости обновлять `aria-valuetext` сразу при движении
- [x] 2.4 Вручную проверить в одной среде AT (NVDA+Firefox или VoiceOver), что слышны проценты, а не «0.x»; при несоответствии уточнить только формат строки

## 3. Спеки (после кода)

- [x] 3.1 Слияние дельт из `openspec/changes/.../specs/` в `openspec/specs/` (или по процессу проекта) после реализации
