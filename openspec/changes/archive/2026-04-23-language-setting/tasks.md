## 1. Стили языковых кнопок

- [x] 1.1 Добавить CSS-блок `.ifk-lang-group` / `.ifk-lang-btn` / `.ifk-lang-btn--active` в `src/style.css` по образцу `.ifk-theme-group`
- [x] 1.2 Добавить `.ifk-lang-group` в правило адаптивной раскладки (`width: 100%` для узких экранов) рядом с `.ifk-theme-group`

## 2. Проверка разметки

- [x] 2.1 Убедиться, что `buildLangRow()` возвращает пустую строку, когда `_languages.length < 2`
- [x] 2.2 Убедиться, что при двух и более языках секция содержит `role="group"` с кнопками по одной на каждый код языка

## 3. Проверка логики переключения

- [x] 3.1 Убедиться, что клик на кнопку языка вызывает `setLanguage(lang)`, сохраняет настройки и вызывает `rerender()`
- [x] 3.2 Убедиться, что `syncToDOM` устанавливает класс `ifk-lang-btn--active` на правильную кнопку при открытии модалки
- [x] 3.3 Убедиться, что сброс настроек возвращает язык к `authorDefaults.language` и обновляет активную кнопку
