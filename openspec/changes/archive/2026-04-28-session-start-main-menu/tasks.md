## 1. Типы и конфигурация игры

- [x] 1.1 Добавить в `GameConfig` / `defineGame` опциональное поле `meta` (`title`, опционально `description`) и провалидировать типы.
- [x] 1.2 Обновить образцовую игру (`game.ts`) и локали: передать `meta`; при необходимости добавить ключи в `template.ui.json` и реальные `*.ui.json`.

## 2. Сессионное состояние и инициализация движка

- [x] 2.1 Ввести состояние «меню сессии» vs «игра» и флаг первого показа при загрузке страницы (`engine.ts`/оркестратор).
- [x] 2.2 Отложить запуск `runGameLoop` до «Новая игра» или успешной загрузки слота; убедиться, что до этого не вызываются сценовые аудиохуки без необходимости.

## 3. Разметка и стили

- [x] 3.1 Расширить `src/index.html` контейнером слоя стартового меню (или создавать узел при init), не ломая `#controls` и `#content`.
- [x] 3.2 Реализовать модуль/UI стартового меню: центрированная колонка (логотип, заголовок из `meta`, описание, кнопки Lucide Plus / как в навбаре).
- [x] 3.3 CSS: скрывать `#controls` при активном меню; теминг согласованно с `--ifk-*`.

## 4. Обработчики меню и интеграция с сохранениями и настройками

- [x] 4.1 Реализовать открытие модалки сохранений с режимом «только загрузка» (API `openSavesModal`/эквивалент); скрыть сохранение/перезапись и связанный UI в этом режиме.
- [x] 4.2 Подключить кнопку «Загрузить» меню к этому режиму; после успешной загрузки перейти в режим «игра» и показать сцену.
- [x] 4.3 Подключить «Настройки» к существующему открытию модалки настроек из кода меню.

## 5. Логотип навбара, автосохранение, аудио

- [x] 5.1 Сделать логотип интерактивным (`button`), задать `aria-label` через `u()`/`UiKey`.
- [x] 5.2 По клику на логотип: вызвать автосохранение в авто-слот; перевести аудио в состояние «нет музыки в меню» без изменения пользовательских настроек mute/volume.
- [x] 5.3 Показать стартовое меню после пункта 5.2, скрыв навбар и игровой контент согласно спекам.

## 6. Строки интерфейса и ключи

- [x] 6.1 Добавить ключи в `src/ifKit/ui-keys.ts`, строки в `builtin-ui.ts`/`BUILTIN_UI` и шаблон локалей для новых подписей.
- [x] 6.2 При необходимости обновить документацию автора одной строкой про `meta` и главное меню.

## 7. Верификация

- [x] 7.1 Ручная проверка: первая загрузка — меню; «Новая игра» — навбар и сцена; «Загрузить» из меню — только загрузка; навбар — полная модалка сохранений.
- [x] 7.2 Проверка аудио: в меню тихо; после выхода в игру музыка ведёт себя по сцене и настройкам; настройки пользователя не порчены фактом входа в меню.
- [x] 7.3 Прогнать линтер/сборку и при необходимости поправить a11y порядок фокуса в меню и при возврате в игру.
