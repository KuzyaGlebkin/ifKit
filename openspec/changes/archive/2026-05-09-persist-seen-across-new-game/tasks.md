## 1. Сессионное меню

- [x] 1.1 В `src/ifKit/session-main-menu.ts` убрать вызов `resetSeenContent()` из обработчика кнопки «Новая игра» (`#ifk-session-menu-new`).
- [x] 1.2 Удалить неиспользуемый импорт `resetSeenContent` из того же файла.

## 2. Проверка и регрессии

- [x] 2.1 Убедиться, что `resetSeenContent` по-прежнему вызывается только из `settings-modal` (кнопка сброса истории прочтения) и при необходимости из публичного API `index.ts`; других вызовов при старте новой игры нет.
- [x] 2.2 Вручную: после просмотра текста начать «Новая игра» — ранее просмотренные блоки с тем же HTML не должны снова получать `paragraph--unseen` (при включённой подсветке). После «Сбросить историю прочтения» в настройках подсветка для текущей сцены снова полная, как раньше.

## 3. Спека после внедрения (опционально)

- [x] 3.1 После `/opsx:apply` при архивации изменения смержить дельты `seen-content` и `session-main-menu` в `openspec/specs/` по процессу проекта.
