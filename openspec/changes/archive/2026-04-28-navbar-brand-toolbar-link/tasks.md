## 1. Разметка и стили навбара

- [x] 1.1 В `src/index.html` перенести блок логотипа внутрь `<ul class="ifk-controls-actions">` как первый `<li>`; заменить `<button id="btn-brand" …>` на `<a id="btn-brand" href="#" …>` с теми же классами логотипа и декоративным SVG; сохранить четыре следующих `<li>` с кнопками и их `id`.
- [x] 1.2 В `src/style.css` задать раскладку `.ifk-controls-actions` как flex на всю ширину `#controls`, с визуальным отделением бренда слева и группы четырёх кнопок справа (например `margin-inline-end: auto` на первом `li`); перенести/адаптировать правила `#btn-brand` для якоря (без подчёркивания, курсор, отступы, hover/focus); убрать устаревший `margin-inline-start: auto` у `ul`, если он конфликтует с новой схемой; проверить медиаправило скрытия логотипа на очень узком экране.

## 2. Поведение и доступность

- [x] 2.1 В `src/ifKit/session-main-menu.ts` на `#btn-brand` вешать обработчик(и): `click` с `preventDefault()` и прежней логикой меню; при необходимости `keydown` для **Space** на ссылке, чтобы не ухудшить клавиатурный паритет с бывшей кнопкой.
- [x] 2.2 Сохранить установку локализуемого `aria-label` (или согласованную замену) на элементе бренда и работу селектора `#btn-brand .ifk-brand-logo-svg` для клона в меню.

## 3. Проверка

- [x] 3.1 Ручная проверка: широкий и узкий viewport, Tab-ордер (ссылка → четыре кнопки), активация бренда из игры (автосохранение, тишина в меню, стартовое меню), отсутствие скачков прокрутки при активации.
- [x] 3.2 Эквивалент CI: `npm run lint` (0 errors) и `npx tsc --noEmit` — скрипта `check` в `package.json` нет.
