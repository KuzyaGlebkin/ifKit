## 1. Рендер `goto` как ссылка

- [x] 1.1 В `flushGotosToDOM` создавать `<a>` с `class="btn-goto"`, осмысленным `href` (например `#`) и обработчиком `click`, который вызывает `preventDefault` и существующий `handler`.
- [x] 1.2 Добавить обработчик `keydown` для Space (и при необходимости уточнить Enter), чтобы активация с клавиатуры совпадала с прежними кнопками и не вызывала побочных эффектов браузера.

## 2. Стили

- [x] 2.1 В `src/style.css` для `.btn-goto` (на `<a>`) сбросить оформление ссылки по умолчанию (`text-decoration` и др.), не меняя цвета, отступы и поведение в flex/mobile из текущих правил.

## 3. Проверка

- [x] 3.1 Вручную: сцена с `act` и `goto` — визуально без регрессий; в DevTools у goto тег `A`; клик, Enter и Space переводят как раньше.
- [x] 3.2 При наличии сборки/линтера — `npm run build` (или эквивалент проекта) без ошибок.
