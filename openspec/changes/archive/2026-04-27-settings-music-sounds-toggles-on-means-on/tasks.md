## 1. UI: инверсия чекбоксов в модалке настроек

- [x] 1.1 В `settings-modal.ts` задавать `musicMute` / `soundMute` checkbox: `checked = !settings.musicMuted` / `!settings.soundMuted` при открытии и обновлении.
- [x] 1.2 В обработчиках `change` записывать `musicMuted: !checked` / `soundMuted: !checked` и по-прежнему вызывать `setMusicMuted` / `setSoundMuted` с загруженными булевыми флагами.
- [x] 1.3 Проверить все пути, где выставляются чекбоксы (сброс, импорт, `commit`), на согласованность с инверсией.

## 2. i18n и доступность

- [x] 2.1 Обновить `ui-keys.ts` (переименовать ключи `*Mute` → `*Enabled` или оставить идентификаторы по решению, зафиксированному в коде) и все ссылки в шаблоне модалки / `data-ifk-ui-aria`.
- [x] 2.2 Обновить `builtin-ui.ts` (ru/en) и при необходимости `chrome-i18n.ts` так, чтобы строки описывали включение, а не отключение.
- [x] 2.3 Убедиться, что подписи/aria у чекбоксов согласованы с `settings-modal` и i18n-спеком.

## 3. Спецификации и проверка

- [x] 3.1 После реализации прогнать сценарии вручную: галочка стоит ↔ музыка/звуки слышны при сохранённой громкости; снятие галочки ↔ mute; ползунок при «mute» не затирает номинал.
- [x] 3.2 Архивировать/слить дельты в корневые `openspec/specs/*` по процессу репозитория после подтверждения (если применяется в этой же сессии).
